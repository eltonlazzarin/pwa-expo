import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  repo: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 20,
  },

  avatar: {
    width: 48,
    height: 48,
    borderRadius: 24,
    marginRight: 16,
  },
});

export default styles;
